//
//  AppColor.swift
//  MoviesApp
//
//  Created by Angela Tasikj on 17.4.26.
//

import SwiftUI

enum AppColor {
    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.8)
    static let tertiaryText = Color.white.opacity(0.6)
    static let mutedText = Color.white.opacity(0.4)
    static let accent = Color.yellow
    static let surfaceOverlay = Color.white.opacity(0.14)
    static let placeholderFill = Color.white.opacity(0.1)
    static let blackOpacity = Color.black.opacity(0.4)
}
